# codeforces templates and algorithims 

### Contest 
Start a new session with `./init --write`. This creates `pA.cpp` through `pF.cpp`. 

Resume a session with `./init` (ommiting `--write`).

Compile and run with `cin` by using `F9`.
